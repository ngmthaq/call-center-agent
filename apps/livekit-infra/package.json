{
  "name": "livekit-infra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "docker compose -f docker-compose.development.yml --env-file .env.development up -d",
    "dev:down": "docker compose -f docker-compose.development.yml --env-file .env.development down"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "dependencies": {},
  "devDependencies": {}
}
